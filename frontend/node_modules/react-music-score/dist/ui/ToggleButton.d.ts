import React from 'react';
interface ButtonProps {
    children?: any;
    className?: string;
    id?: string;
    onClick?: (e: React.MouseEvent<HTMLButtonElement>) => void;
    checked?: boolean;
    disabled?: boolean;
}
export declare const ToggleButton: ({ children, className, id, checked, onClick, disabled, }: ButtonProps) => JSX.Element;
export {};
