/// <reference types="react" />
interface ButtonProps {
    children?: any;
    disabled?: boolean;
    className?: string;
    id?: string;
    onClick?: () => void;
}
export declare const Button: ({ children, disabled, className, id, onClick, }: ButtonProps) => JSX.Element;
export {};
